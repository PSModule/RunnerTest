on: push

jobs:
  test:
    strategy:
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Run a multi-line script
        shell: pwsh
        run: |
          $PSVersionTable

  test2:
    runs-on: windows-latest
    steps:
      - name: Run a multi-line script
        shell: powershell
        run: |
          $PSVersionTable
